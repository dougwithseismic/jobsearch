{
  "title": "Workable Jobs Scraper",
  "description": "Discover, scrape, and search jobs from 1,700+ companies using Workable as their ATS. Auto-discovers companies — no manual URL input needed.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "mode": {
      "title": "Mode",
      "type": "string",
      "description": "How to run the scraper:\n- 'all' — Auto-discover ALL companies and scrape every job board. Zero config.\n- 'companies' — Scrape only the company slugs you specify.\n- 'search' — Scrape specific companies and immediately filter results.",
      "editor": "select",
      "enum": ["all", "companies", "search"],
      "default": "all",
      "prefill": "all",
      "sectionCaption": "Scraping Mode",
      "sectionDescription": "Choose how you want to discover and scrape Workable job boards."
    },
    "companies": {
      "title": "Company Slugs",
      "type": "array",
      "description": "Workable job board slugs to scrape. Used in 'companies' and 'search' modes.\n\nThe slug is the path segment in apply.workable.com/{slug}. Examples: '1000heads', 'typeform', 'spotify'.\n\nNot sure of the slug? Run in 'all' mode first — the output includes every slug.",
      "editor": "stringList",
      "default": [],
      "sectionCaption": "Target Companies",
      "sectionDescription": "Specify which companies to scrape. Leave empty for 'all' mode."
    },
    "remoteOnly": {
      "title": "Remote Jobs Only",
      "type": "boolean",
      "description": "Only return jobs explicitly marked as remote (telecommuting) by the company.",
      "default": false
    },
    "locationFilter": {
      "title": "Location Filter",
      "type": "string",
      "description": "Regex pattern to match job locations (country, city, state).\n\nExamples:\n- 'europe|germany|uk|france' — European jobs\n- 'remote' — Any location containing 'remote'\n- 'new york|san francisco|austin' — US tech hubs\n- 'prague|czech' — Specific city/country",
      "editor": "textfield",
      "default": ""
    },
    "departmentFilter": {
      "title": "Department Filter",
      "type": "string",
      "description": "Regex pattern to match departments.\n\nExamples:\n- 'engineering|product' — Tech roles\n- 'sales|marketing|growth' — Go-to-market\n- 'design|creative' — Design teams\n- 'data|machine.learning|ai' — Data/ML roles",
      "editor": "textfield",
      "default": ""
    },
    "keywordFilter": {
      "title": "Title/Keyword Filter",
      "type": "string",
      "description": "Regex pattern to match job titles.\n\nExamples:\n- 'senior|staff|lead|principal' — Senior roles\n- 'full.?stack|fullstack' — Full-stack engineers\n- 'react|typescript|node' — Tech-specific\n- 'founding|head.of|vp|director' — Leadership\n- 'intern|junior|entry' — Entry-level",
      "editor": "textfield",
      "default": ""
    },
    "includeDescriptions": {
      "title": "Include Full Descriptions",
      "type": "boolean",
      "description": "Include the full HTML job description in output. Note: Workable's widget API does not include descriptions inline — this field is reserved for future per-job fetching.",
      "default": false
    },
    "concurrency": {
      "title": "Concurrency",
      "type": "integer",
      "description": "Number of parallel API requests. Higher = faster scraping but more memory. Recommended: 10 for full scrape, 5 for testing.",
      "default": 10,
      "minimum": 1,
      "maximum": 20
    },
    "maxCompanies": {
      "title": "Max Companies",
      "type": "integer",
      "description": "Limit the number of companies to scrape. 0 = no limit (scrape all). Useful for testing or staying within budget.",
      "default": 0,
      "minimum": 0
    }
  },
  "required": ["mode"]
}
